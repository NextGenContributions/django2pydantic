[pytest]
addopts =
    --cov=django2pydantic
    --cov=tests
    --cov-branch
    --cov-report=term-missing
    --cov-report=xml
    --cov-report=lcov:lcov.info
    --junitxml=junit.xml
testpaths = tests
